<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wyc.provider.mapper.OrderMapper">
  <insert id="addOrder">
    insert into `seata_order` (`id`, `user`, `commodity_code`, `money`)
    values (null, #{user}, #{commodityCode}, #{money})
  </insert>
</mapper>